import { addHeaderToApp } from './header.js';
import { addCarsToApp } from './cars.js';

const app = document.body;

// prideda header į app
// addHeaderToApp(app);

// prideda cars į app
// addCarsToApp(app);